(define (problem depth_2_agents_2)
	(:domain grapevine)
	(:requirements :typing)
	(:objects l1 l2 - loc)
	(:projection )
	(:depth 2)
	(:task valid_generation)
	(:init-type complete)
	(:init 
		[] >> (connected_l1_l2)
		[[BEL, bob]] >> (secret_bob)
		[[BEL, alice]] >> (secret_alice)
		[] >> (at_alice_l1)
		[] >> (at_bob_l2)
		[] >> (connected_l2_l1)
	)
	(:goal 
		[[BEL, alice]] >> (secret_bob)
	)
	(:plan 
	)
)